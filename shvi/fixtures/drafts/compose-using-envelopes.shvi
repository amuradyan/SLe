; here we work in Lisp-like musical syntax

(
  (define instrument
    (lambda (adr passage)
      (plays the passage under adr)))

  (define piano
    (lambda (passage)
      (instrument (adr 0.2 0.1 0.1) passage)))

  (piano (tone C4 2)))